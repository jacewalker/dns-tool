<div class="container-fluid pt-3">
    <div class="row">
        <div class="col-4 offset-4">
            <h1 class="col-4 offset-3 w-50 fw-light">DNS Lookup</h1>
            <div class="text-muted">
            <p class="py-1">This tool will complete a full DNS lookup on the provided domain.</p>
            </div>
            <div class="px-5">
                <%= form_with do |form| %>
                    <% form.label :query, "Domain Name:", class:"form-label" %>
                    <%= form.text_field :domain, class:"form-control", placeholder: "apple.com.au" %>
                    <%= form.submit "Go", class: "btn btn-primary mt-3 col-4 offset-4", style: "width: 100px" %>
                <% end %>

                <%# <form action='/lookup' method='post' accept-charset="UTF-8" class='form-control'>
                    <label class="form-label text-muted" for="domain">Enter your domain</label>
                    <input class="form-control" type="text" id="domain" name="domain"> <br>

                    <input class="btn btn-primary col-4 offset-4" type="submit" value="Submit">
                </form> %>



            </div>
            <p> 
                <%=  %>
            </p>
        </div>
    </div>
</div>
